"use strict";

class Squarer {
  getSquares(numbers) {
    return numbers.map(num => {
      return {
        number: num,
        square: num * num
      };
      num * num;
    });
  }
}
module.exports = new Squarer()
